{
  "name": "pb-backend",
  "version": "1.0.0",
  "description": "backend for phonebook application",
  "license": "ISC",
  "author": "Elmo Käppi",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd ../../part2/phonebook && npm run build && cp -r dist ../../part3/pb-backend/build",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.18.2",
    "mongoose": "^8.19.3",
    "morgan": "^1.10.1"
  },
  "engines": {
    "node": "22.x"
  }
}
